#include "main.h"
/**
 * _isupper - evalue if letter is uppercase .
 * @l: print int
 * Return: Always 0.
 */

int _isupper(int l)
{
	if (l >= 'A' && l <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
